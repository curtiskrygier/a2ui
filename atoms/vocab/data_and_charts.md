---
category: data_and_charts
title: Data, Charts & Metrics
atom_count: 34
platform_support:
  web: 34/34
  meet-stage: 0/34
  googlechat: 0/34
  google-apps-script-web: 34/34
  email: 0/34
  pdf: 0/34
maturity: stable
source: atoms/schema.yaml
---

# Data, Charts & Metrics

> 34 atoms · `*` = degraded (limited support) · Bold surface = full support

| Atom type | Description | Surfaces | Key fields |
|-----------|-------------|----------|------------|
| `stat_card` | single KPI value with label delta and accent colour indicator | **Web/Blog** · **GAS Web App** | `value` — string<br>`label` — string<br>`delta` — string (optional)<br>`is_up` — bool (optional)<br>`accent` — string (optional) |
| `metric_delta` | big number metric with directional change indicator or percentage | **Web/Blog** · **GAS Web App** · **Email** · **PDF** | `label` — string (descriptive label for the metric, e.g., 'Sales')<br>`current_value` — string (the current formatted value, e.g., '$12,345')<br>`delta_value` — string (the formatted change value, e.g., '+$1,234' or '+10%')<br>`delta_type` — enum (increase, decrease, no_change) - determines icon/color<br>`unit` — string (optional, unit for the current value, e.g., 'USD')<br>`previous_period_label` — string (optional, e.g., 'vs. last month') |
| `progress_bar` | labelled horizontal progress bar with percentage | **Web/Blog** · **GAS Web App** | `value` — integer (0-100)<br>`label` — string<br>`accent` — string (optional)<br>`show_percent` — bool (optional)<br>`caption` — string (optional) |
| `progress_circle` | CSS stroke-dashoffset animated SVG progress arc with centre percentage | **Web/Blog** · **GAS Web App** | `value` — integer. The progress value (0–100).<br>`label` — string (optional). Caption text below the circle.<br>`color` — string (optional). Arc stroke colour. Default "#38bdf8".<br>`size` — "sm" | "md" | "lg"  (optional, default "md"). Circle diameter. |
| `sparkline` | inline SVG trend line chart general trends. | **Web/Blog** · **GAS Web App** | `data` — list of numbers (e.g., [10, 12, 8, 15, 13])<br>`color` — string (hex or named color, e.g., '#4CAF50' or 'green')<br>`line_width` — number (e.g., 2)<br>`height` — string (CSS height value, e.g., '20px')<br>`width` — string (CSS width value, e.g., '80px') |
| `mini_sparkline_set` | compact grid of multiple labeled sparklines | **Web/Blog** · **GAS Web App** | `series` — array of {label, data}. Sparkline series. |
| `trend_indicator` | compact inline value with mini trend bar the direction of a | **Web/Blog** · **GAS Web App** · **Email** · **PDF** | `trend_direction` — enum (up, down, stable) - determines the icon or arrow direction<br>`label` — string (descriptive text for the trend, e.g., 'Improving', 'Declining', 'Steady')<br>`context` — string (optional, additional context, e.g., 'over last 30 days')<br>`color` — string (optional, hex or named color for the indicator, e.g., 'green' for 'up', 'red' for 'down') |
| `heatmap` | grid heatmap coloured by intensity value are represented as colors in a matrix. | **Web/Blog** · **GAS Web App** | `data` — list of lists of numbers (e.g., [[1, 2, 3], [4, 5, 6]])<br>`labels_x` — list of strings (labels for the x-axis, e.g., ['Mon', 'Tue', 'Wed'])<br>`labels_y` — list of strings (labels for the y-axis, e.g., ['AM', 'PM'])<br>`color_scale` — list of strings (colors for the gradient, e.g., ['#FFFFFF', '#FF0000'] for white to red)<br>`unit` — string (optional, unit for the data values, e.g., '°C') |
| `heatmap_calendar` | calendar month heatmap with date-value density colouring | **Web/Blog** · **GAS Web App** · **PDF** | `title` — string (optional). Heading above the calendar grid.<br>`data` — array. List of {date, count} objects where date is "YYYY-MM-DD".<br>`months` — integer (optional). Number of calendar months to render. Default 3.<br>`color_scale` — array of strings (optional). CSS colours from empty to maximum density. Default IBM Carbon blue scale.<br>`unit` — string (optional). Unit label appended to count in tooltips, e.g. "commits". |
| `punch_card` | 7x24 grid of glowing activity bubbles representing hourly commits | **Web/Blog** · **GAS Web App** | `data` — list of lists of numbers (7 rows representing days of week, each containing 24 numbers representing commit frequency for each hour)<br>`labels_days` — list of strings (optional, e.g., ['Mon', 'Tue', 'Wed', 'Thu', 'Fri', 'Sat', 'Sun'])<br>`color` — string (optional, hex or named color for active bubbles, e.g., '#00f2ff')<br>`title` — string (optional, title of the repository or chart, e.g., 'Commit Activity')<br>`subtitle` — string (optional, subtitle, e.g., 'curtiskrygier/meetstudio') |
| `sankey_flow` | curved dual-column gradient flow diagram for financial or pipeline streams | **Web/Blog** · **GAS Web App** | `nodes` — list of dictionaries representing columns (e.g., [{'id': 'revenue', 'label': 'Revenue', 'column': 0, 'color': '#10b981'}, ...])<br>`links` — list of dictionaries representing flows (e.g., [{'source': 'revenue', 'target': 'marketing', 'value': 25000, 'color': '#38bdf8'}, ...])<br>`title` — string (optional, title of the chart, e.g., 'Financial Flow Allocation') |
| `cohort_retention` | triangular cohort retention grid with continuous color-scale rendering | **Web/Blog** · **GAS Web App** | `cohorts` — list of dictionaries representing cohorts (e.g., [{'cohort_name': 'Jan 2026', 'original_size': 2500, 'retention_rates': [100.0, 93.4]}])<br>`periods` — list of strings representing period headers (e.g., ['Month 0', 'Month 1'])<br>`color_scale` — list of strings representing the continuous color scale (e.g., ['#1e293b', '#10b981'])<br>`title` — string (optional, chart title, e.g., 'SaaS Retention Cohorts') |
| `donut_stat` | CSS ring chart with centre value and percentage its proportion or progress. | **Web/Blog** · **GAS Web App** | `value` — number (the current value, e.g., 75)<br>`max_value` — number (the maximum possible value, e.g., 100)<br>`label` — string (descriptive label for the metric, e.g., 'Completion')<br>`unit` — string (optional, unit for the value, e.g., '%')<br>`color` — string (hex or named color for the donut segment, e.g., '#2196F3')<br>`size` — string (CSS size value, e.g., '100px') |
| `gauge_sla` | radial SLA gauge with glowing pointer and large metric value | **Web/Blog** · **GAS Web App** | `title` — string (optional, e.g., 'API Response SLA')<br>`value` — number representing current value (e.g., 99.4)<br>`max_value` — number representing maximum gauge scale (e.g., 100)<br>`unit` — string (optional, e.g., '%')<br>`label` — string (optional, e.g., 'SLA Met') |
| `stacked_area` | stacked area chart with overlapping glowing translucent gradients | **Web/Blog** · **GAS Web App** | `title` — string (optional, e.g., 'SaaS Cumulative Workloads')<br>`labels` — list of strings representing X-axis ticks (e.g., ['Q1', 'Q2', 'Q3', 'Q4'])<br>`series` — list of dictionaries representing stacked layers: [{'label': 'Enterprise', 'data': [10, 25, 45, 60], 'color': '#00f2ff'}, ...] |
| `scatter_trend` | coordinate scatter plot with regression trend line | **Web/Blog** · **GAS Web App** | `title` — string (optional, e.g., 'CSAT vs. Support Response Time')<br>`data_points` — list of [x, y] coordinates: [[1.2, 95], [2.5, 88], [3.1, 75]]<br>`label_x` — string (optional, e.g., 'Response Time (Hours)')<br>`label_y` — string (optional, e.g., 'CSAT Score') |
| `chartjs_bar` | interactive Chart.js bar chart with datasets | **Web/Blog** · **GAS Web App** | `labels` — array of strings. X-axis labels.<br>`datasets` — array of {label, data}. Chart datasets. |
| `chartjs_line` | interactive Chart.js line chart for trend data | **Web/Blog** · **GAS Web App** | `labels` — array of strings. X-axis labels.<br>`datasets` — array of {label, data}. Chart datasets. |
| `chartjs_pie` | multi-slice pie or donut SVG chart with legend | **Web/Blog** · **GAS Web App** · **PDF** | `title` — string (optional). Chart heading.<br>`variant` — string (optional). "pie" or "donut". Default "donut".<br>`data` — List of {label, value} slice objects.<br>`colors` — array of strings (optional). Hex colours per slice; auto-assigned if omitted.<br>`show_legend` — boolean (optional). Show colour-keyed legend below chart. Default true.<br>`show_labels` — boolean (optional). Show percentage labels on slices. Default true. |
| `data_table_sortable` | sortable data table with column headers | **Web/Blog** · **GAS Web App** | `headers` — array of strings. Column headers.<br>`rows` — array of arrays. Table rows. |
| `data_grid` | enterprise data grid with typed columns, row selection, and | **Web/Blog** · **GAS Web App** · **PDF** | `title` — string (optional). Dark header bar above the grid.<br>`columns` — array. List of {header, key, type?, sortable?}. type is one of string | number | status | tag.<br>`rows` — array. List of row objects where each key matches a column key.<br>`selectable` — boolean (optional). Show row-selection checkboxes. Default false.<br>`pagination` — object (optional). '{per_page} splits rows into CSS-tab pages; clicking a page number label switches pages without JavaScript. Omit for single-page display.' |
| `metric_comparison_card` | metric card comparing current vs previous period | **Web/Blog** · **GAS Web App** | `label` — string. Metric name.<br>`value` — number. Current value.<br>`previous` — number. Previous period value. |
| `benchmark_comparison` | side-by-side performance benchmark comparison bars | **Web/Blog** · **GAS Web App** | `items` — array of {label, value}. Benchmark entries. |
| `feature_matrix` | features versus plans comparison matrix | **Web/Blog** · **GAS Web App** · **PDF** | `product_names` — array<br>`features` — array |
| `comparison_grid` | multi-item attribute comparison grid often using icons or checkmarks to indicate presence. | **Web/Blog** · **GAS Web App** · **PDF** | `products` — array<br>`features` — array |
| `versus_block` | head-to-head two-option versus card "VS" separator. | **Web/Blog** · **GAS Web App** | `entity_a_name` — string<br>`entity_a_description` — string<br>`entity_a_image_url` — string<br>`entity_b_name` — string<br>`entity_b_description` — string<br>`entity_b_image_url` — string<br>`comparison_points` — array |
| `rating_comparison` | multiple items rated across dimensions scores. | **Web/Blog** · **GAS Web App** · **Email** · **PDF** | `items` — array |
| `conversion_funnel` | pipeline conversion funnel with step tapering and leak markers | **Web/Blog** · **GAS Web App** | `title` — string (optional, e.g., 'Acquisition Funnel')<br>`steps` — list of dictionaries representing funnel stages: [{'stage': 'Visits', 'value': 10000}, {'stage': 'Signups', 'value': 4500}, ...] |
| `uptime_timeline` | 30-day uptime timeline with per-day status blocks | **Web/Blog** · **GAS Web App** | `uptime` — number. Uptime percentage (0–100). |
| `status_dashboard` | grid of service status indicators with color states | **Web/Blog** · **GAS Web App** | `metrics` — array of {label, value, color}. Status entries. |
| `github_activity_grid` | SVG GitHub-style contribution grid and activity tracker | **Web/Blog** · **GAS Web App** | `title` — string (optional, e.g., 'GitHub Repository Activity')<br>`username` — string (optional, e.g., 'curtiskrygier')<br>`total_contributions` — integer (optional, e.g., 342)<br>`streak_days` — integer (optional, e.g., 18)<br>`activity` — list of integers (0-4) or dictionary of weeks/days for rendering grid squares |
| `github_repo_card` | live GitHub repo card with stars forks and language | **Web/Blog** · **GAS Web App** | `repo` — string<br>`label` — string<br>`description` — string |
| `repo_links` | styled GitHub repo link list | **Web/Blog** · **GAS Web App** · **Email** · **PDF** | `links` |
| `llm_comparison_table` | side-by-side multi-model output comparison table | **Web/Blog** · **GAS Web App** · **PDF** | `prompt` — string (optional). The shared input prompt shown above the comparison.<br>`models` — List of {name, output, latency_ms?, cost_usd?, tokens?} model result objects.<br>`show_meta` — boolean (optional). Show latency/cost/token row below each output. Default true if any model provides meta fields. |
