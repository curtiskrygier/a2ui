---
category: interactive_and_ui
title: Interactive UI & Forms
atom_count: 38
platform_support:
  web: 38/38
  meet-stage: 32/38
  googlechat: 1/38
  google-apps-script-web: 38/38
  email: 0/38
  pdf: 0/38
maturity: stable
source: atoms/schema.yaml
---

# Interactive UI & Forms

> 38 atoms · `*` = degraded (limited support) · Bold surface = full support

| Atom type | Description | Surfaces | Key fields |
|-----------|-------------|----------|------------|
| `tabs` | tabbed content panels | **Web/Blog** · **Meet Stage** · **GAS Web App** | `accent` — string (optional)<br>`tabs` |
| `tab_bar` | horizontal tab navigation bar typically leading to different sections or pages. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `tabs` — list of {label: string, url: string, is_active: boolean, icon: string (optional, e.g., 'home')} |
| `stepper` | CSS animated vertical step sequence with checkmark draw and active pulse | **Web/Blog** · **Meet Stage** · **GAS Web App** | `steps` — list of strings or {label, description} objects. Each step in the sequence.<br>`active_index` — integer (optional, default 0). Index of the currently executing step. Steps before it are completed; steps after are pending.<br>`color` — string (optional). Accent colour for completed/active states. Default "#38bdf8".<br>`label` — string (optional). Heading above the step list. |
| `segmented_control` | mutually exclusive option selector strip as a single control. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `options` — list of objects with label, value<br>`selected_value` — string<br>`name` — string |
| `toggle_switch` | CSS toggle switch input | **Web/Blog** · **Meet Stage** · **GAS Web App** | `label` — string<br>`is_checked` — boolean<br>`name` — string |
| `flip_card` | card that flips on hover to reveal back content | **Web/Blog** · **Meet Stage** · **GAS Web App** | `front_content` — string<br>`back_content` — string<br>`trigger_on_hover` — boolean |
| `tooltip` | hover tooltip on a trigger element over a specified trigger element, using only CSS. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `trigger_text` — string The text or element that, when hovered, reveals the tooltip.<br>`tooltip_content` — string The content displayed within the tooltip. |
| `hover_card` | rich hover card revealed on mouse over specified trigger element, using only CSS. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `trigger_element` — string The text or element that, when hovered, reveals the card.<br>`card_title` — string The title of the hover card.<br>`card_content` — string The main content of the hover card, can include rich text or simple HTML.<br>`card_image_url` — string, optional An optional image to display within the card. |
| `css_modal` | pure-CSS modal dialog triggered by checkbox controlled purely by CSS without JavaScript. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `trigger_text` — string The text or element that, when clicked, opens the modal.<br>`modal_title` — string The title displayed at the top of the modal.<br>`modal_body` — string The main content of the modal dialog.<br>`close_button_label` — string, default "Close" The label for the button to close the modal. |
| `css_dropdown_menu` | pure-CSS dropdown navigation menu | **Web/Blog** · **Meet Stage** · **GAS Web App** | `trigger_text` — string<br>`menu_items` — list of objects with label, url |
| `css_slide_panel` | CSS-only slide-in panel drawer activation. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `trigger_text` — string<br>`panel_content` — string<br>`slide_from_side` — string |
| `modal` | modal dialog overlay with title and content | **Web/Blog** · **Meet Stage** · **GAS Web App** | `title` — string. Modal heading.<br>`size` — string (optional). One of: sm, md, lg. Default: md.<br>`children` — Content atoms to render inside the modal body.<br>`trigger_label` — string (optional). If set, renders a button that opens the modal. |
| `poll_block` | poll with question options and vote count bars | **Web/Blog** · **GAS Web App** | `question` — string. Poll question.<br>`options` — array of {text, votes}. Poll choices. |
| `vote_button_group` | Multi-option vote buttons in pill, neon, or default style | **Web/Blog** · **Meet Stage** · **GAS Web App** | `options` — array of objects — label (string), value (string), selected (boolean optional).<br>`style` — string (optional). One of pill, neon, default. Default pill.<br>`title` — string (optional). Section heading above the buttons.<br>`allow_multi` — boolean (optional). Allow multiple selections. Default false. |
| `rating_stars` | star rating display stars, or displaying a static rating. | **Web/Blog** · **Meet Stage** · **GAS Web App** | `rating` — integer The current rating value (e.g., 3 for 3 stars).<br>`max_rating` — integer The maximum possible rating (e.g., 5 for 5 stars).<br>`is_interactive` — boolean Indicates if the stars are clickable for user input. |
| `star_rating_input` | interactive star rating input | **Web/Blog** · **Meet Stage** · **GAS Web App** | `max_stars` — integer<br>`initial_rating` — integer<br>`name` — string |
| `star_rating_display` | read-only star rating with score and count total review count. | **Web/Blog** · **Meet Stage** · **GAS Web App** · **PDF** | `rating` — number<br>`max_rating` — integer<br>`review_count` — integer |
| `rating_summary_bar` | star rating histogram with percentage bars per star level | **Web/Blog** · **Meet Stage** · **Google Chat** · **GAS Web App** · **Email** · **PDF** | `average` — number. Overall average rating (e.g. 4.3).<br>`total` — number. Total number of ratings.<br>`breakdown` — List of {stars, count} from 5 down to 1.<br>`accent` — string (optional, default |
| `custom_checkbox_group` | styled checkbox group input | **Web/Blog** · **Meet Stage** · **GAS Web App** | `group_label` — string<br>`options` — list of objects with label, value, is_checked<br>`name` — string |
| `form` | labelled form with fields and submit button | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `title` — string (optional). Form heading shown above fields.<br>`submit_label` — string (optional, default 'Submit'). Label for the primary submit button.<br>`cancel_label` — string (optional). If set, renders a secondary cancel button.<br>`fields` — array |
| `form_input` | single labelled input field | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string. Visible label above the input.<br>`name` — string. Field identifier.<br>`type` — string (optional). One of: text, email, password, number, url. Default: text.<br>`placeholder` — string (optional).<br>`rules` — array of strings (optional). e.g. ['required', 'email', 'minLength:2']. |
| `form_select` | labelled dropdown select with options | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string. Visible label.<br>`name` — string. Field identifier.<br>`placeholder` — string (optional). e.g. 'Select an option…'<br>`options` — List of {value, label} pairs.<br>`rules` — array of strings (optional). e.g. ['required']. |
| `form_radio_group` | radio button group for single-option selection | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Group label.<br>`name` — string. Field identifier.<br>`options` — List of {value, label, description?} entries.<br>`default_value` — string (optional). Pre-selected option value.<br>`rules` — array of strings (optional). e.g. ['required']. |
| `form_checkbox_group` | checkbox group for multi-option selection | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Group label.<br>`name` — string. Aggregate field identifier for form submission.<br>`items` — List of {name, label, description?, default_checked?} entries.<br>`rules` — array of strings (optional). |
| `form_switch_group` | group of labelled toggle switches | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Section label above the switches.<br>`name` — string. Aggregate field identifier.<br>`items` — List of {name, label?, description?, default_checked?} entries. |
| `form_slider` | numeric range slider input | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Visible label above the slider.<br>`name` — string. Field identifier.<br>`min` — number. Minimum value.<br>`max` — number. Maximum value.<br>`step` — number (optional). Increment size. Omit for continuous.<br>`default_value` — number (optional). Initial thumb position.<br>`variant` — string (optional). One of: continuous, discrete. Default: continuous.<br>`rules` — array of strings (optional). |
| `form_date_picker` | date or date-range picker input | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Visible label.<br>`name` — string. Field identifier.<br>`mode` — string (optional). One of: single, range. Default: single.<br>`placeholder` — string (optional). e.g. 'Pick a date'.<br>`rules` — array of strings (optional). e.g. ['required']. |
| `combobox` | searchable filterable dropdown — shadcn/Headless UI pattern | **Web/Blog** · **Meet Stage** · **GAS Web App** | `label` — string (optional). Visible label above the field.<br>`name` — string. Field identifier.<br>`placeholder` — string (optional). Input placeholder text.<br>`options` — array of {value, label} pairs.<br>`selected` — string (optional). Pre-selected option value.<br>`rules` — array of strings (optional). e.g. ['required']. |
| `multi_select_input` | chip-style multi-value select input — shadcn pattern | **Web/Blog** · **Meet Stage** · **GAS Web App** | `label` — string (optional). Visible label above the field.<br>`name` — string. Field identifier.<br>`placeholder` — string (optional). Input placeholder when no chips are selected.<br>`options` — array of {value, label} pairs.<br>`selected` — array of value strings. Pre-selected values shown as chips. |
| `otp_input` | N-box one-time password digit input — shadcn OTP pattern | **Web/Blog** · **GAS Web App** | `length` — integer (optional). Number of digit boxes. Default 6.<br>`value` — string (optional). Pre-filled digits.<br>`label` — string (optional). Visible label above the boxes. |
| `variant_selector` | CSS radio card group for variant or option picking | **Web/Blog** · **Meet Stage** · **GAS Web App** | `name` — string. Form field name used in radio input group.<br>`label` — string (optional). Group label shown above the options.<br>`items` — List of {value, title, description?} option objects.<br>`default_value` — string (optional). Pre-selected option value. |
| `choicebox_group` | card-style option selector with icon, title, description per card | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `label` — string (optional). Group heading above the cards.<br>`name` — string. Form field name for the selected value(s).<br>`multiple` — boolean (optional, default false). If true allows multiple selections.<br>`accent` — string (optional, default<br>`submit_label` — string (optional). If set, renders a submit button below the cards.<br>`items` — List of {value, title, description?, icon?, disabled?} entries. |
| `follow_up_chips` | clickable follow-up suggestion chips | **Web/Blog** · `Meet Stage*` · **GAS Web App** | `items` — List of suggestion strings. e.g. ['What's the ROI?', 'Show me by region', 'Compare to last year'].<br>`label` — string (optional). Small heading above the chips. e.g. 'You might also ask:' |
| `search_result_card` | single search result card with title snippet URL | **Web/Blog** · **Meet Stage** · **GAS Web App** | `title` — string. Result title.<br>`description` — string. Snippet text.<br>`url` — string (optional). Result URL. |
| `command_palette` | keyboard-driven command palette with shortcut hints | **Web/Blog** · **GAS Web App** | `commands` — array of {text, shortcut}. Available commands. |
| `navigation_menu` | multi-level horizontal nav with submenu panels — Radix pattern | **Web/Blog** · **GAS Web App** | `brand` — string (optional). Brand name shown on the left.<br>`brand_url` — string (optional). URL for the brand link.<br>`items` — array of objects with — label, url, children (optional array of {label, url, description}).<br>`cta` — object (optional). {label, url} for a right-aligned call-to-action button. |
| `scroll_to_top` | quickly return viewport to top of page document | **Web/Blog** · **GAS Web App** | `behavior` — string: smooth | auto. Scroll animation profile. |
| `reading_progress_bar` | track and display article reading progress completion status | **Web/Blog** · **GAS Web App** | `color` — string. Hexadecimal or design utility color token for the bar. |
